---
description: 
globs: 
alwaysApply: true
---
This is First Person Dungeon Crawler game written in Lua using the Love2D framework
In Lua function finish with the end keyword and not with }
When adding new UI elements that need callbacks, do not forget to check if the main.lua file has the proper callbacks in place, if not, add it
Remember that if you are using a function/value from another file/module, you need to import it using thre require statement
Remember that you are using Lua, a language that do not have types and can have nil values, if you are not sure a value has been initialized, check for nil before using it in any operation
Do not try to run any script/program inside the project if you were not explicitly asked to
You should always try your best to support Mouse and Keyboard on any feature you implement
We have a balance dashboard at [balance_dashboard.html](mdc:dev/templates/balance_dashboard.html) and [balance_analyzer.py](mdc:dev/balance_analyzer.py) and a content manager dashboard at [manage.html](mdc:dev/templates/manage.html) and [app.py](mdc:dev/app.py), everytime a change affects how content (content in everything at /data folder, but for balance analyzer is anything that affects combat damage calculations) is structured or a new field is added, you should review the impact on those dashboards and update them.

Do not commit code unless you were specifically asked to
You are not allowed to run the game
Never try to run the game
You should never test the code by executing terminal/console commands